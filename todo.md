Create Python project with pytest structure

Implement base classes for:

API testing (using pytest + requests)

UI testing (using pytest + Selenium)

Add Healenium integration (Python wrapper or custom logic)
